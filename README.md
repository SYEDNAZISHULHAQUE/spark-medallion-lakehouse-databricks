# spark-medallion-lakehouse-databricks
This repository demonstrates a scalable and modular data engineering pipeline built with PySpark on Databricks, leveraging AWS cloud services for storage and processing. The pipeline follows the Medallion architecture (Bronze → Silver → Gold), enabling efficient data ingestion, transformation, and analytics.
